---
import Layout from '../layouts/Layout.astro';
import Card from '../components/Card.astro';
import workItems from '../data/work.json';
---

<Layout title="Work — Ibrahim Saad">
  <div class="container">
    <header class="page-header">
      <h1>Work</h1>
      <p>Professional experience and things I've contributed to.</p>
    </header>

    <div class="card-list" id="card-list">
      {workItems.map(item => <Card {...item} />)}
    </div>
  </div>
</Layout>

<script>
  import { initCardFilters } from '../scripts/filters';
  initCardFilters(document.getElementById('card-list')!);
</script>
